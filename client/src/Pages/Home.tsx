import React from 'react';
import RestaurantList from '../Components/Organisms/RestaurantList';

const Home: React.FC = () => {
  return (
    <div>
      <RestaurantList />
    </div>
  );
};

export default Home;
