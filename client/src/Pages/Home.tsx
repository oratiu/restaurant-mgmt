import React from 'react';
import RestaurantList from '../components/RestaurantList.tsx';

const Home: React.FC = () => {
  return (
    <div>
      <RestaurantList />
    </div>
  );
};

export default Home;
